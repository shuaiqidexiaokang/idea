server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1/springboot2
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml #扫描mapper配置文件
  config-location: classpath:mybatis-config.xml #扫描mybatis全局配置文件
  type-aliases-package: com.lzk.springboot2.model #扫描model包
